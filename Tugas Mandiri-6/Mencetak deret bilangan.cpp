#include <stdio.h>

int main() {
    int i;
    
    // Menggunakan perulangan untuk mencetak deret bilangan dari 10 hingga 100 dengan selisih 10
    for (i = 10; i <= 100; i += 10) {
        printf("%d ", i);
    }
    
    return 0;
}
